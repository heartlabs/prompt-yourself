# Build products
.build/
DerivedData/

# Swift Package Manager local state
.swiftpm/

# Xcode user-specific settings
xcuserdata/

# macOS filesystem metadata
.DS_Store

# LLM API configuration (contains secrets — copy from .template files)
llm-config.plist
# API keys bundled in code — flip `active` to switch providers
LLMConfigs.swift
