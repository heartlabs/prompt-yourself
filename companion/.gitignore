# Rust build output
server/target/
# Server runtime state (VAPID keys, subscriptions, schedule) — keep it private
companion-state.json
server/companion-state.json
# Dev-only test dependency (npm install --no-save jsdom)
node_modules/
package-lock.json
